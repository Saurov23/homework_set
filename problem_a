#include <stdio.h>

int main() {
    int a, b, perimeter;
    printf("enter side a : ");
    scanf("%d", &a);
    printf("enter side b : ");
    scanf("%d", &b);
    perimeter = 2 * (a + b);
    printf("perimeter of rectangle = %d\n", perimeter);
    return 0;
}
